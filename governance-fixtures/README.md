# Governance fixtures

This directory contains synthetic test data for governance evaluation only. It is not
application configuration, and its fixture filenames intentionally avoid conventional
dotenv names so the application cannot load them accidentally.

Every credential-like value here must be unmistakably fake, nonfunctional, and safe to
commit.
