{
  "name": "github-channel",
  "version": "0.1.0",
  "description": "GitHub one-way channel for Claude Code — PR events, CI status, issues, reviews",
  "type": "module",
  "scripts": {
    "build": "tsc && chmod 755 build/channel.js",
    "channel": "npm install --no-audit --no-fund && npm run build && node build/channel.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.28.0",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^24.0.1",
    "typescript": "^5.8.3"
  }
}
